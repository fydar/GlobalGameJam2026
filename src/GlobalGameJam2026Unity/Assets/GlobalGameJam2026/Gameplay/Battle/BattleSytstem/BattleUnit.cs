using UnityEngine;

public class BattleUnit : MonoBehaviour
{
    int health;
    int defence;
    int attack;
    int magic;
    int speed;
    int critChance;
    int actionPoints;

    BattleTile gridPosition;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
